import { UnredaedMsgPipe } from './unredaed-msg.pipe';

describe('UnredaedMsgPipe', () => {
  it('create an instance', () => {
    const pipe = new UnredaedMsgPipe();
    expect(pipe).toBeTruthy();
  });
});
